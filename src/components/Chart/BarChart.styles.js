import { createStyles } from '@mantine/core';

const useStyles = createStyles((theme) => ({
  title: {
    marginBottom: 24,
  },

  chart: {
    position: 'relative',
    width: '99%',
    height: 200,
  },
}));

export default useStyles;
